// use faer::{Col, Mat};

fn main() {
    // const OUTPUTS: usize = 3; //32;
    // const INPUTS: usize = 2; //64;
    // let mut mat = Mat::zeros(OUTPUTS, INPUTS);
    // for i in 0..OUTPUTS {
    //     for j in 0..INPUTS {
    //         mat[(i, j)] = 1.0; //(i + j) as f64;
    //     }
    // }
    // let mut remainder = mat.clone();
    // let mut approximat = Mat::zeros(OUTPUTS, INPUTS);
    // let mut optimal_input = Col::zeros(INPUTS);
    // let mut optimal_output = Col::zeros(OUTPUTS);
    // let mut test_input = Col::zeros(INPUTS);
    // let mut test_output = Col::zeros(OUTPUTS);
    // let mut rng = rand::thread_rng();
    // println!("{}", remainder.norm_l2());
    todo!();
    // for i in 0..5 {
    //     let new_cut = cut_mat_signed(
    //         remainder.as_mut(),
    //         approximat.as_mut(),
    //         optimal_input.as_mut(),
    //         optimal_output.as_mut(),
    //         test_input.as_mut(),
    //         test_output.as_mut(),
    //         &mut rng,
    //         10,
    //         100,
    //     );
    //     println!("i = {i}: {}, {}", remainder.norm_l2(), new_cut.cut_value);
    //     println!("{remainder:?}");
    // }
}
